# LOTTO SCIENTIFIC - 분자운동 기반 확률 분석 시스템
# 필수 의존성 패키지

# 핵심 라이브러리
pygame>=2.6.0              # 3D 분자운동 시뮬레이션 엔진
numpy>=1.24.0              # 수치 계산 및 배열 처리
matplotlib>=3.7.0          # 과학적 데이터 시각화
pandas>=2.0.0              # 로또 당첨번호 데이터 분석

# 과학 계산
scipy>=1.10.0              # 통계적 열역학 계산
scikit-learn>=1.3.0        # 머신러닝 기반 패턴 분석

# 웹 개발 (선택사항)
flask>=2.3.0               # 웹 API 서버 (고급 기능용)
requests>=2.31.0           # HTTP 클라이언트 (외부 데이터 연동)

# 개발 도구 (requirements-dev.txt에서 분리 가능)
pytest>=7.4.0             # 단위 테스트 프레임워크
black>=23.7.0              # 코드 포매터
flake8>=6.0.0              # 코드 스타일 검사
mypy>=1.5.0                # 정적 타입 검사

# 문서화
sphinx>=7.1.0              # API 문서 생성
sphinx-rtd-theme>=1.3.0    # ReadTheDocs 테마
